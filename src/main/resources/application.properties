
# ===========================================
# QUESTCONNECT API CONFIGURATION
# ===========================================

# Application Name
spring.application.name=Questconnect

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Port pour QuestConnect (Gateway/Security)
server.port=8080
# Contexte de l'application - toutes les routes seront préfixées
server.servlet.context-path=/questconnect

# ===========================================
# BACKEND API CONFIGURATION
# ===========================================
# URL de votre API métier witchertoolkit-back
toolkit.api.base.url=http://localhost:8081/witcher-toolkit

# ===========================================
# JWT CONFIGURATION
# ===========================================
# Clé secrète pour signer les tokens JWT (minimum 256 bits pour HS256)
jwt.secret=ChangeThisSecretToAStrongRandomStringForProduction1234567890123456789012345678901234567890
# Durée de vie du token en millisecondes (24h = 86400000ms)
jwt.expiration=86400000
# Header HTTP pour le token
jwt.header=Authorization
# Préfixe du token dans le header
jwt.prefix=Bearer 

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Origins autorisées (votre front Angular)
cors.allowed.origins=http://localhost:4200
# Méthodes HTTP autorisées
cors.allowed.methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
# Headers autorisés (* pour simplicité, ou spécifiez : Content-Type,Authorization,X-Requested-With)
cors.allowed.headers=*
# Permettre l'envoi de cookies/credentials
cors.allow.credentials=true
# Temps de cache pour les requêtes preflight OPTIONS
cors.max.age=3600

# ===========================================
# LOGGING CONFIGURATION
# ===========================================
logging.level.root=INFO
logging.level.fr.meya.witcher=DEBUG
# Logs détaillés pour le JWT (temporaire pour debug)
logging.level.org.springframework.security=DEBUG

# ===========================================
# SPRING BOOT ACTUATOR (Monitoring)
# ===========================================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always